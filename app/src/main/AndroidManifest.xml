<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luka2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PripremaPreferences"
            android:label="@string/title_activity_priprema_preferences"></activity>
        <activity
            android:name=".activity.Screen"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PripremaListActivity"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.SecondActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="Films"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="Films"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.PripremaDetail"
            android:icon="@mipmap/ic_launcher"
            android:label="Pogledani Filmovi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".preferences.PripremaPrefererences"
            android:label="@string/activity_settings_name"
            android:parentActivityName=".activity.PripremaListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>